node_modules/
# Ignore every .env variant (.env, .env1, .env.local, .env.production ...)
# but keep the committed template. Order matters: the negation must come last.
.env*
!.env.example
.vercel
*.log

# Firebase service account keys — NEVER commit these
*firebase-adminsdk*.json
*service-account*.json

# Plaintext API key dumps — NEVER commit these
geminikeys.txt
*keys*.txt
