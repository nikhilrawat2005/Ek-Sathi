# Project Rules (from Nikhil)

## Git workflow
- Always commit after every set of changes.
- Push to `origin` / `main` automatically after committing.
- Never commit secrets or the local `.env` file (only `.env.example`).
